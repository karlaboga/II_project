<Window x:Class="BengosRestaurantApp.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Staff Login"
        Height="400" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#EEEDED">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Staff Management System" FontSize="20" FontWeight="Bold"
                   Foreground="#3D3B3A" HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <!-- Username -->
        <TextBlock Grid.Row="1" Text="Username:" Foreground="#464443" Margin="0,0,0,5"/>
        <TextBox Grid.Row="2" x:Name="TxtUsername" Height="30" FontSize="14"
                 Background="#FFFFFF" BorderBrush="#CCCBCB"/>

        <!-- Password -->
        <TextBlock Grid.Row="3" Text="Password:" Foreground="#464443" Margin="0,15,0,5"/>
        <PasswordBox Grid.Row="4" x:Name="TxtPassword" Height="30" FontSize="14"
                     Background="#FFFFFF" BorderBrush="#CCCBCB"/>

        <!-- Login Button -->
        <Button Grid.Row="5" x:Name="BtnLogin" Content="Login" Height="40" FontSize="14" FontWeight="Bold"
                Background="#3E3C3B" Foreground="#FFFFFF" BorderThickness="0"
                Margin="0,20,0,0" Click="BtnLogin_Click"/>

        <!-- Error Message -->
        <TextBlock Grid.Row="6" x:Name="LblError" Foreground="#C83232" HorizontalAlignment="Center"
                   Margin="0,10,0,0" Text=""/>
    </Grid>
</Window>
