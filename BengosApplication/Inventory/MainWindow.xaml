<Window x:Class="Inventory.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Inventory - Bengos Restaurant"
        Height="650" Width="1000"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="#F3EDEE"
        FontFamily="Segoe UI"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType">

    <Window.Resources>
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#5E3E4A"/>
            <Setter Property="Foreground" Value="#F3EDEE"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="10"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#7A5A68"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#B88FA1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#F3EDEE" CornerRadius="22" Margin="10">
        <Grid Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Inventory Item" FontSize="20" FontWeight="Bold" Foreground="#5E3E4A" Margin="5,0,0,15"/>

                <TextBlock Text="Product Name" Foreground="#7A5A68" FontWeight="SemiBold" Margin="5"/>
                <TextBox Text="{Binding ProductName, UpdateSourceTrigger=PropertyChanged}" Margin="5" Background="#F3EDEE" Foreground="#5E3E4A" BorderBrush="#C9A3B5"/>

                <TextBlock Text="Category" Foreground="#7A5A68" FontWeight="SemiBold" Margin="5"/>
                <ComboBox Text="{Binding Category, UpdateSourceTrigger=PropertyChanged}" Margin="5" Background="#F3EDEE" Foreground="#5E3E4A" BorderBrush="#C9A3B5">
                    <ComboBoxItem Content="Vegetables"/>
                    <ComboBoxItem Content="Fruits"/>
                    <ComboBoxItem Content="Herbs &amp; Aromatics"/>
                    <ComboBoxItem Content="Beef &amp; Pork"/>
                    <ComboBoxItem Content="Poultry"/>
                    <ComboBoxItem Content="Seafood &amp; Fish"/>
                    <ComboBoxItem Content="Plant Proteins"/>
                    <ComboBoxItem Content="Dairy"/>
                    <ComboBoxItem Content="Eggs"/>
                    <ComboBoxItem Content="Oils &amp; Fats"/>
                    <ComboBoxItem Content="Bakery"/>
                    <ComboBoxItem Content="Dry Grains &amp; Pastas"/>
                    <ComboBoxItem Content="Spices"/>
                    <ComboBoxItem Content="Canned"/>
                </ComboBox>

                <TextBlock Text="Quantity" Foreground="#7A5A68" FontWeight="SemiBold" Margin="5"/>
                <TextBox Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" Margin="5" Background="#F3EDEE" Foreground="#5E3E4A" BorderBrush="#C9A3B5"/>

                <TextBlock Text="Unit" Foreground="#7A5A68" FontWeight="SemiBold" Margin="5"/>
                <ComboBox Text="{Binding Unit, UpdateSourceTrigger=PropertyChanged}" Margin="5" Background="#F3EDEE" Foreground="#5E3E4A" BorderBrush="#C9A3B5">
                    <ComboBoxItem Content="Kilograms"/>
                    <ComboBoxItem Content="Liters"/>
                    <ComboBoxItem Content="Pieces"/>
                    <ComboBoxItem Content="Packs"/>
                </ComboBox>

                <TextBlock Text="Min Stock" Foreground="#7A5A68" FontWeight="SemiBold" Margin="5"/>
                <TextBox Text="{Binding MinStock, UpdateSourceTrigger=PropertyChanged}" Margin="5" Background="#F3EDEE" Foreground="#5E3E4A" BorderBrush="#C9A3B5"/>

                <StackPanel Margin="5,15,5,0">
                    <Button Content="Add" Style="{StaticResource RoundedButtonStyle}" Margin="5" Command="{Binding AddCommand}" Height="35"/>
                    <Button Content="Edit" Style="{StaticResource RoundedButtonStyle}" Margin="5" Background="#7A5A68" Command="{Binding EditCommand}" Height="35"/>
                    <Button Content="Delete" Style="{StaticResource RoundedButtonStyle}" Margin="5" Background="#C94A4A" Command="{Binding DeleteCommand}" Height="35"/>
                    <Button Content="Clear Filter" Style="{StaticResource RoundedButtonStyle}" Margin="5" Background="#C9A3B5" Foreground="#5E3E4A" Command="{Binding ClearFilterCommand}" Height="35"/>
                    <Button Content="Close" Style="{StaticResource RoundedButtonStyle}" Margin="5,40,5,0" Background="#8A6B7A" Click="BtnClose_Click" Height="35" Width="95"/>
                </StackPanel>
            </StackPanel>

            <Grid Grid.Column="1" Margin="15,0,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="5,0,5,15">
                    <TextBlock Text="Filter by Category:" 
                               Foreground="#5E3E4A" 
                               FontWeight="Bold" 
                               FontSize="14" 
                               VerticalAlignment="Center" 
                               Margin="0,0,10,0"/>

                    <ComboBox SelectedValue="{Binding FilterCategory, UpdateSourceTrigger=PropertyChanged}" 
                              SelectedValuePath="Content"
                              Width="180" Height="28" Background="#F3EDEE" Foreground="#5E3E4A" BorderBrush="#C9A3B5" VerticalContentAlignment="Center">
                        <ComboBoxItem Content="All"/>
                        <ComboBoxItem Content="Vegetables"/>
                        <ComboBoxItem Content="Fruits"/>
                        <ComboBoxItem Content="Herbs &amp; Aromatics"/>
                        <ComboBoxItem Content="Beef &amp; Pork"/>
                        <ComboBoxItem Content="Poultry"/>
                        <ComboBoxItem Content="Seafood &amp; Fish"/>
                        <ComboBoxItem Content="Plant Proteins"/>
                        <ComboBoxItem Content="Dairy"/>
                        <ComboBoxItem Content="Eggs"/>
                        <ComboBoxItem Content="Oils &amp; Fats"/>
                        <ComboBoxItem Content="Bakery"/>
                        <ComboBoxItem Content="Dry Grains &amp; Pastas"/>
                        <ComboBoxItem Content="Spices"/>
                        <ComboBoxItem Content="Canned"/>
                    </ComboBox>
                </StackPanel>

                <DataGrid Grid.Row="1" 
                          ItemsSource="{Binding Products}" 
                          SelectedItem="{Binding SelectedProduct, UpdateSourceTrigger=PropertyChanged}"
                          AlternationCount="2"
                          AutoGenerateColumns="False" 
                          IsReadOnly="True" 
                          Background="#F3EDEE" 
                          BorderBrush="#C9A3B5" 
                          Foreground="#5E3E4A"
                          HeadersVisibility="Column">

                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="#F3EDEE"/>
                            <Setter Property="Foreground" Value="#5E3E4A"/>

                            <Style.Triggers>
                                <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                                    <Setter Property="Background" Value="#E9DDE5"/>
                                </Trigger>

                                <DataTrigger Binding="{Binding IsLowStock}" Value="True">
                                    <Setter Property="Background" Value="#f54949"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>

                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#5E3E4A"/>
                            <Setter Property="Foreground" Value="#F3EDEE"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </Style>
                    </DataGrid.Resources>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding ProductName}" Width="*"/>
                        <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="*"/>
                        <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="*"/>
                        <DataGridTextColumn Header="Unit" Binding="{Binding Unit}" Width="*"/>
                        <DataGridTextColumn Header="Min" Binding="{Binding MinStock}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>

        </Grid>
    </Border>
</Window>