<Window x:Class="Kitchen.CookBook"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Kitchen CookBook" Height="650" Width="1000"
MinHeight="650" MinWidth="1000"
WindowStartupLocation="CenterScreen"
ResizeMode="NoResize"
Background="#F3EDEE"
WindowStyle="None">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Background="#F3EDEE" CornerRadius="22" Margin="10">

            <Grid Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,15">
                    <TextBlock Text="Filter Category:" FontSize="14" FontWeight="SemiBold" Foreground="#5E3E4A" Margin="0,0,0,5"/>

                    <ComboBox x:Name="CategoryFilterComboBox"
          Height="35"
          VerticalContentAlignment="Center"
          SelectedIndex="0"
          SelectionChanged="CategoryFilterComboBox_SelectionChanged"
          BorderBrush="#C9A3B5"
          Background="#F3EDEE">

                        <ComboBox.Style>
                            <Style TargetType="ComboBox">
                                <Setter Property="BorderBrush" Value="#C9A3B5"/>
                                <Setter Property="Background" Value="#F3EDEE"/>
                                <Setter Property="Foreground" Value="#5E3E4A"/>
                                <Setter Property="Padding" Value="6,0,0,0"/>

                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ComboBox">
                                            <Grid>
                                                <ToggleButton x:Name="ToggleButton"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          Background="{TemplateBinding Background}"
                                          Grid.Column="2" 
                                          Focusable="false"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                                    <ToggleButton.Template>
                                                        <ControlTemplate TargetType="ToggleButton">
                                                            <Border x:Name="Border" 
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="1"
                                                CornerRadius="4"/>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter TargetName="Border" Property="Background" Value="#E9DDE5"/>
                                                                    <Setter TargetName="Border" Property="BorderBrush" Value="#3E2630"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </ToggleButton.Template>
                                                </ToggleButton>

                                                <ContentPresenter x:Name="ContentSite"
                                              IsHitTestVisible="False" 
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"/>

                                                <Path x:Name="Arrow"
                                  Grid.Column="2"     
                                  Fill="#5E3E4A"
                                  HorizontalAlignment="Right" 
                                  VerticalAlignment="Center"
                                  Margin="0,0,10,0"
                                  Data="M 0 0 L 4 4 L 8 0 Z"/>

                                                <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True" 
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                                    <Grid x:Name="DropDown"
                                      SnapsToDevicePixels="True"
                                      MinWidth="{TemplateBinding ActualWidth}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                                        <Border x:Name="DropDownBorder"
                                            Background="#FFFFFF"
                                            BorderThickness="1"
                                            BorderBrush="#C9A3B5"
                                            CornerRadius="0,0,4,4"/>
                                                        <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                                        </ScrollViewer>
                                                    </Grid>
                                                </Popup>
                                            </Grid>

                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsKeyboardFocused" Value="True">
                                                    <Setter TargetName="ToggleButton" Property="BorderBrush" Value="#4A2F39"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ComboBox.Style>

                        <ComboBox.ItemContainerStyle>
                            <Style TargetType="ComboBoxItem">
                                <Setter Property="Padding" Value="8,6"/>
                                <Setter Property="Foreground" Value="#5E3E4A"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="FontSize" Value="14"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ComboBoxItem">
                                            <Border x:Name="ItemBorder" 
                                Background="{TemplateBinding Background}" 
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                                                <ContentPresenter />
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="ItemBorder" Property="Background" Value="#E9DDE5"/>
                                                    <Setter Property="Foreground" Value="#3E2630"/>
                                                </Trigger>
                                                <Trigger Property="IsHighlighted" Value="True">
                                                    <Setter TargetName="ItemBorder" Property="Background" Value="#7A5A68"/>
                                                    <Setter Property="Foreground" Value="#F3EDEE"/>
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="ItemBorder" Property="Background" Value="#7A5A68"/>
                                                    <Setter Property="Foreground" Value="#F3EDEE"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ComboBox.ItemContainerStyle>

                        <ComboBoxItem Content="All Dishes"/>
                        <ComboBoxItem Content="Appetizers"/>
                        <ComboBoxItem Content="Main Course"/>
                        <ComboBoxItem Content="Drinks"/>
                        <ComboBoxItem Content="Desserts"/>

                    </ComboBox>
                </StackPanel>
                <TextBlock Grid.Row="1" 
           Text="Search Dish:" 
           FontSize="14" 
           FontWeight="SemiBold" 
           Foreground="#5E3E4A" 
           Margin="0,0,0,5"/>

                <TextBox Grid.Row="2"
         x:Name="SearchTextBox"
         TextChanged="SearchTextBox_TextChanged"
         Height="35"
         VerticalContentAlignment="Center"
         Padding="5"
         Margin="0,0,0,15"
         BorderBrush="#C9A3B5"
         Background="#F3EDEE">

                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Background" Value="#F3EDEE"/>
                            <Setter Property="BorderBrush" Value="#C9A3B5"/>
                            <Setter Property="Foreground" Value="#5E3E4A"/>
                            <Setter Property="CaretBrush" Value="#3E2630"/>
                            <Setter Property="SelectionBrush" Value="#7A5A68"/>
                            <Setter Property="BorderThickness" Value="1"/>

                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TextBox">
                                        <Border x:Name="TextBoxBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">

                                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Focusable="false" 
                                          HorizontalScrollBarVisibility="Hidden" 
                                          VerticalScrollBarVisibility="Hidden"/>
                                        </Border>

                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="TextBoxBorder" Property="BorderBrush" Value="#7A5A68"/>
                                                <Setter Property="Background" Value="#E9DDE5"/>
                                            </Trigger>

                                            <Trigger Property="IsKeyboardFocused" Value="True">
                                                <Setter TargetName="TextBoxBorder" Property="BorderBrush" Value="#3E2630"/>
                                                <Setter TargetName="TextBoxBorder" Property="BorderThickness" Value="2"/>
                                                <Setter Property="Background" Value="#FFFFFF"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </TextBox.Style>
                </TextBox>
                <ListBox Grid.Row="3"
                         x:Name="RecipeListBox"
                         Background="Transparent"
                         BorderThickness="0"
                         DisplayMemberPath="Name">

                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="10"/>
                            <Setter Property="FontSize" Value="16"/>
                            <Setter Property="Foreground" Value="#5E3E4A"/>
                            <Setter Property="Background" Value="Transparent"/>

                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="ListItemBorder" 
                            Background="{TemplateBinding Background}" 
                            Padding="{TemplateBinding Padding}">
                                            <ContentPresenter />
                                        </Border>

                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="ListItemBorder" Property="Background" Value="#E9DDE5"/>
                                                <Setter Property="Foreground" Value="#3E2630"/>
                                            </Trigger>

                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="ListItemBorder" Property="Background" Value="#3E2630"/>
                                                <Setter Property="Foreground" Value="#F3EDEE"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </Grid>
        </Border>

        <Grid Grid.Column="1" Margin="25">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock x:Name="TxtDishName"
                               Text="Select a Recipe"
                               FontSize="28"
                               FontWeight="Bold"
                               Foreground="#5E3E4A"/>

                    <WrapPanel Margin="0,8,0,0" Height="34">
                        <TextBlock Text="⏱️ Prep Time: " FontSize="14" Foreground="#7A5A68" FontWeight="Bold"/>
                        <TextBlock x:Name="TxtPrepTime" Text="--" FontSize="14" Foreground="#7A5A68"/>
                        <TextBlock Text="⚠️ Allergens: " FontSize="14" Foreground="#C94A4A" FontWeight="Bold"/>
                        <TextBlock x:Name="TxtAllergens" Text="--" FontSize="14" Foreground="#C94A4A"/>
                    </WrapPanel>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top">

                    <Button x:Name="BtnAddRecipe"
                            Content="➕ Add Recipe"
                            Click="BtnAddRecipe_Click"
                            Width="120"
                            Height="35"
                            Margin="0,0,10,0"
                            FontSize="13"
                            FontWeight="SemiBold"
                            Background="#7A5A68"
                            Foreground="#F3EDEE"
                            BorderThickness="0">

                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="border"
                                        Background="{TemplateBinding Background}">
                                    <ContentPresenter HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"/>
                                </Border>

                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border"
                                                Property="Background"
                                                Value="#3E2630"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <Button x:Name="BtnBack"
                            Content="⬅️ Back"
                            Click="BtnBack_Click"
                            Width="80"
                            Height="35"
                            FontSize="13"
                            FontWeight="SemiBold"
                            Background="#C9A3B5"
                            Foreground="#5E3E4A"
                            BorderThickness="0">

                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="border"
                                        Background="{TemplateBinding Background}">
                                    <ContentPresenter HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"/>
                                </Border>

                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border"
                                                Property="Background"
                                                Value="#3E2630"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                </StackPanel>
            </Grid>

            <Separator Grid.Row="1" Background="#E9DDE5" Height="2" Margin="0,15"/>

            <StackPanel Grid.Row="2" Margin="0,0,0,20">
                <TextBlock Text="INGREDIENTS" FontSize="16" FontWeight="Bold" Foreground="#7A5A68" Margin="0,0,0,8"/>
                <TextBlock x:Name="TxtIngredients" Text="No ingredients to display." FontSize="15" Foreground="#5E3E4A"/>
            </StackPanel>

            <DockPanel Grid.Row="3">
                <TextBlock DockPanel.Dock="Top" Text="PREPARATION INSTRUCTIONS" FontSize="16" FontWeight="Bold" Foreground="#7A5A68" Margin="0,0,0,8"/>
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="TxtInstructions"
                               Text="Select a dish from the sidebar menu to read instructions."
                               FontSize="15"
                               Foreground="#5E3E4A"/>
                </ScrollViewer>
            </DockPanel>

        </Grid>
    </Grid>
</Window>